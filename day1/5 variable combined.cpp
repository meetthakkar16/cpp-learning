#include <iostream>;

using namespace std; 

int main() {
    int a; 
    int b;
    float c = 0.0, d = 1.1; // two variables of same data type can be declared by using ',' in one single line instead of two lines

    cin >> a >> b; 

    cout << "Value of a is " << a << endl;
    cout << "Value of b is " << b << endl;

    return 0;

}
